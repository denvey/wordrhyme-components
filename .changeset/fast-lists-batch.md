---
'@wordrhyme/auto-crud-server': minor
---

Run list and export count queries concurrently, apply stable multi-column sorting, preserve indexable text and JSON search targets, and add bulk CRUD extension persistence with a backward-compatible per-row fallback. Batch extension writes now require stable unique ids so returned rows are never associated by position.
